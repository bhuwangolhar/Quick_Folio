# API Configuration
# For development (optional - defaults to /api):
VITE_API_URL=http://localhost:5000/api

# For production with different backend server:
# VITE_API_URL=https://your-backend-api.com/api

# For production with same-origin deployment (leave empty or don't set):
# VITE_API_URL=/api
